-----------------IMPORTANT-----------------------------------------

NOTE 1:

Before running the solution, we need to update the paths (location) for the below variables. Currently the paths
are set for my system. The new paths should be compatible with the newly saved location of the project.

Broweser
ExcelPath
NavigationScreenShot
PhotoPath
ReportPath
ReportXML
ScreenshotPath
RowCount

NOTE 2:

This project contains test cases for NUNIT and Specflow (both). All testcases which have names ending with 'old' are NUNIT
test cases, and rest are Specflow test cases. Both are working. 